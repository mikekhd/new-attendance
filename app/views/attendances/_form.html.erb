<%= simple_form_for(@attendance) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :user_id %>
    <%= f.input :login %>
    <%= f.input :logout %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
